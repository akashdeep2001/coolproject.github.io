function setup() {
  createCanvas(windowWidth, windowHeight, )
}

function draw() {

  // head of the stickman with colour
  fill(255,255,255)
  ellipse(200, 200, 100, 100)

  // hat
  fill(255, 0,0)
  rect(150, 120, 100, 30)
  rect(125,70)
}
